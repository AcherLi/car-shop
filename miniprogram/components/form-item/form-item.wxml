<view class="label-item {{label ? '' : 'normal'}}">
  <view class="left" wx:if="label">
    <view class="required">{{required ? '*' : ''}}</view>
    <view class="label">{{label}}</view>
  </view>
  <view class="{{label ? 'right' : 'left-in'}}  {{type}}">
    <slot></slot>
  </view>
</view>
